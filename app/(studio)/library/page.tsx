import { ComingSoon } from "@/components/shell/coming-soon";

export default function LibraryPage() {
  return (
    <ComingSoon
      title="The library lands in week 2."
      when="Week 2"
      blurb="A view of everything the team has shipped through a playbook. Files live in OneDrive, links open from here."
    />
  );
}

import { ComingSoon } from "@/components/shell/coming-soon";

export default function SchoolsPage() {
  return (
    <ComingSoon
      title="Schools directory lands in week 3."
      when="Week 3"
      blurb="The schools GSL works with, programmes active, last touch. Edited directly in OneDrive meta/schools.json."
    />
  );
}

import { ComingSoon } from "@/components/shell/coming-soon";

export default function SkillsPage() {
  return (
    <ComingSoon
      title="Skills list lands in week 3."
      when="Week 3"
      blurb="The frameworks behind every playbook. NEP, NCF, Bloom's, brand voice, partner norms. Each playbook links into the skills it uses."
    />
  );
}
